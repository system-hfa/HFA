# HFA Opus Findings Action Matrix

| finding_id | severity | opus_evidence | confirmed | action | implementation_status | test_evidence | remaining_risk |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | critical | `DELETE /api/events/[eventId]` sem gate admin | yes | trocar `requireBearerUser` por `requireAdmin` e bloquear hard delete | implemented | `tests/sera-vnext/event-deletion/hard-delete-disabled-trial-001.ts` | purge fora de `DRY_RUN` segue bloqueado |
| F-002 | critical | delete sem audit durável | yes | registrar `event.hard_delete_denied`, `event.deletion_requested`, `event.soft_deleted`, `event.restored`, `event.purge_started`, `event.purge_scheduled` como eventos críticos | implemented | leitura de `frontend/src/lib/observability/audit.ts` e trilhas dos endpoints | ainda faltam eventos de purge real |
| F-003 | critical | exclusão irreversível | yes | modelo com `deleted_at`, `deletion_status`, `recoverable_until` e restauração | implemented | rotas `delete-request` e `restore`; migration `20260609123000_safe_event_deletion_lifecycle.sql` | purge definitivo não liberado |
| F-005 | high | risco de cascade destruir ações corretivas | yes | bloquear delete quando existirem ações `pending` ou `in_progress` | implemented | `tests/sera-vnext/event-deletion/event-delete-corrective-action-block-trial-001.ts` | FKs antigos continuam existentes; proteção atual é via endpoint/lifecycle |
| F-006 | high | storage sem garantia de inventário/purge segura | yes | inventário por `source_file_url` e purge só em `DRY_RUN` | implemented | `tests/sera-vnext/event-deletion/event-delete-storage-dry-run-trial-001.ts` | sem purge real automatizada nesta fase |
| DEMO-EXEC | high | `/reports/executive` exibia dados demo em rota produtiva | yes | substituir por carregamento de dados reais do tenant | implemented | inspeção da rota atual | depende de sessão autenticada |
| DEMO-EVENT | high | `/reports/event/[id]` caía em fallback demo silencioso | yes | remover demo fallback e responder `Dados indisponíveis` | implemented | inspeção da rota atual | relatórios sem análise continuam parciais |
| PARITY-001 | high | contagens divergentes entre dashboard/eventos/admin | yes | filtrar `deleted_at is null` e expor contrato formal em `/api/admin/stats` | implemented | `tests/sera-vnext/frontend-data-parity-real-trial-001.ts` | ações corretivas ainda não ocultam vínculo a evento excluído em toda UI |
| ERC-001 | medium | fórmulas ERC espalhadas na UI | yes | reutilizar `frontend/src/lib/risk-profile/erc.ts` nas listas de eventos e relatório individual | partially_implemented | inspeção de `events/page.tsx` e `reports/event/[id]/page.tsx` | tela detalhada de evento ainda preserva cálculo expandido para card rico |
| FILTER-001 | high | consultas ativas sem filtro global de soft delete | yes | aplicar filtro em `/api/events`, `/api/events/[id]`, risco e reanálise | implemented | inspeção de rotas alteradas | outras leituras indiretas ainda precisam auditoria adicional |
| TEST-001 | medium | cobertura insuficiente | yes | adicionar trials direcionados de contrato | implemented | pasta `tests/sera-vnext/event-deletion/` | faltam trials reais de staging/Playwright autenticado |
