# SERA Engine vNext ATLAS-3591 Substantive Patch A4R120 v0.2.0

Status: SUBSTANTIVE_TRACE_PATCH
Phase: A4+R-120
methodology: SERA

## Patch objective
Apply substantive methodological correction before any future review consideration.

## Applied patch points
1. Boundary-path consistency correction (P-axis)
- Removed P-D as boundary-live because selected path has `P_TIME_PRESSURE = NÃO`.
- Registered canonical inaccessibility explicitly in trace.
- Correct boundary kept as P-G (instrument/cue availability versus vestibular/somatogravic conflict).

2. A-axis substantive reframe
- A-C downgraded from apparent closure posture.
- A-axis reframed to `A-F` primary draft with `A-C` and `A-E` live alternatives.
- A-axis status set to patched review-required posture; no closure claim.

3. Multi-actor scope formalization
- `tracedActor` added as `Combined`.
- Rationale recorded for not splitting into two traces in this patch phase.
- PF/PM/combined evidence separation table added.

4. Status controls
- Case kept outside any author review bundle in A4R120.
- Trace marked as patched review-required.
- No release approval and no downstream action.

## Governance effect
ATLAS-3591 remains high-value for P-boundary learning but still requires review work before future bundle eligibility.
