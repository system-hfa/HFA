# Carregado automaticamente em `next build` na Vercel (NODE_ENV=production).
# Garante redirect OAuth com URL fixa mesmo se Supabase rejeitar origin dinâmico.
NEXT_PUBLIC_SITE_URL=https://hfa-omega.vercel.app
